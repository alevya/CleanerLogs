<UserControl x:Class="CleanerLogs.Views.MachineDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CleanerLogs.Views"
             mc:Ignorable="d" 
             Width="300"
             >
    <Grid>

		<Border>
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"></ColumnDefinition>
					<ColumnDefinition Width="Auto"></ColumnDefinition>
					<ColumnDefinition Width="Auto"></ColumnDefinition>
				</Grid.ColumnDefinitions>
				<TextBlock Grid.Column="1"  
				           Text= "{Binding Path=Number}"></TextBlock>
				<TextBlock Grid.Column="2" 
				           Text="{Binding Path=Ip}"></TextBlock>
				<CheckBox Grid.Column="0"
				          IsChecked="{Binding Path=IsSelected, Mode=TwoWay}"
				>
					<!--IsChecked="{Binding Path=IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}}"-->
				</CheckBox>

			</Grid>
		</Border>

	</Grid>
</UserControl>
